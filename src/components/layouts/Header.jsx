import Button from "../ui/Button";

const Header = () => {
  return (
    <header className="bg-primary3 text-primary body-padding-x body-padding-y">
      <Button label={"Book a visit"} />
      Header
    </header>
  );
};

export default Header;
